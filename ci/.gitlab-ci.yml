stages:
  - build
  - run

Build:
  stage: build
  script:
    - docker-compose ./ci/docker-compose.yml build --no-cache
  tags:
    - rnr
#  only:
#    - master
Run:
  stage: run
  script:
    - docker-compose ./ci/docker-compose.yml up
  tags:
    - rnr
#  only:
#    - master
